[package]
name = "gherkin_testcomments"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
gherkin = {version = "0.11", features = ["serde", "parser"]}
